[
  {
    "name": "Reschedule - non-existent ref",
    "steps": [
      {
        "send": { "callback": "booking:reschedule:NONEXIST" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Booking not found. Check the reference code and try again." } }
        ]
      }
    ]
  }
]
